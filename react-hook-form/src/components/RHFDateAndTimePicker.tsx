import { FieldValues, Path } from "react-hook-form"
import { OptionType } from "../users/types/option";

type PropsType<T extends FieldValues> = {
    name: Path<T>;
    options: OptionType[];
}

export function RHFDateAndTimePicker(){
    

    return (
        <>
        </>
    )
}